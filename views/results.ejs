<table class="results">
  <tr>
  <%
    var keys = ['publicId', 'publicStartTime', 'pageIndex', 'publicAuctionId', 'tablesCorrect', 'price', 'droppedOut', 'won', 'totalEarnings', 'netEarnings', 'publicQuestionnaire'];
//    'questionnaire totalEarnings netEarnings questionnaire paymentPerTable showupFee';
    for(k in keys) {
    var key = keys[k];
    %>
    <td><%- key %></td>
    <% } %>
  </tr>
  <%
    for(var r in results) {
    var result = results[r];
  %>
  <tr>
    <% for(k in keys) {
    var key = keys[k];
    %>
    <td>
    <%- result[key] %>
    </td>
    <% } %>
  </tr>
  <% } %>
</table>
