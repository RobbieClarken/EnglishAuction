<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <% if(startTime) { %>
    <p>Experiment started: <%= startTime %></p>
    <p>Subjects: <%= subjectCount %></p>
    <% } else { %>
    <p>No experiment running.</p>
    <% } %>
    <form action="/session/new" method="PUSH">
      <table>
        <tr>
          <td>Subjects per auction</td><td><input type="input" name="groupSize" value="<%= groupSize %>" /></td>
        </tr>
        <tr>
          <td>Auction increment</td><td><input type="input" name="increment" value="<%= increment %>" /></td>
        </tr>
      </table>
      <input type="submit" value="Start New Experiment" />
    </form>
  </body>
</html>
